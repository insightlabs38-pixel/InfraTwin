import type { NetworkProject } from '../../model/src/index.ts';
import { modelHash } from '../../model/src/index.ts';
import { runCapacityAnalysis } from '../../evidence/src/index.ts';
export interface InspectNetworkSummary { projectId: string; name: string; modelHash: string; routingMode: string; nodeCount: number; linkCount: number; demandCount: number; availableLinkCount: number; disabledLinkIds: string[]; verdict: string; peakUtilizationPct: number; violations: Array<{ type: string; linkId?: string; demandId?: string; message: string }>; }
export interface ModelContextLike { registerTool(tool: { name: string; title?: string; description: string; inputSchema?: Record<string, unknown>; annotations?: { readOnlyHint?: boolean; untrustedContentHint?: boolean }; execute: (input: Record<string, never>) => unknown | Promise<unknown>; }, options?: { signal?: AbortSignal }): Promise<void> | void; }
export function inspectNetwork(project: NetworkProject): InspectNetworkSummary { const analysis = runCapacityAnalysis(project); return { projectId: project.id, name: project.name, modelHash: modelHash(project), routingMode: project.routingProfile.mode, nodeCount: project.nodes.length, linkCount: project.links.length, demandCount: project.demands.length, availableLinkCount: project.links.filter((link) => link.available !== false).length, disabledLinkIds: project.links.filter((link) => link.available === false).map((link) => link.id), verdict: analysis.result.verdict, peakUtilizationPct: Number(analysis.result.metrics.peakUtilizationPct), violations: analysis.result.violations.map(({ type, linkId, demandId, message }) => ({ type, linkId, demandId, message })) }; }
export async function registerInspectNetworkTool(context: ModelContextLike, getProject: () => NetworkProject): Promise<() => void> { const controller = new AbortController(); await context.registerTool({ name: 'inspect_network', title: 'Inspect current network', description: 'Reads the currently open InfraTwin network and returns a compact deterministic topology/capacity summary. It does not modify project state.', inputSchema: { type: 'object', properties: {}, additionalProperties: false }, annotations: { readOnlyHint: true }, execute: async () => inspectNetwork(getProject()) }, { signal: controller.signal }); return () => controller.abort(); }
