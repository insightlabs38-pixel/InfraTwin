name: Final Adversarial WebMCP Input Fix

on:
  push:
    branches: [final/adversarial-validation]
    paths:
      - scripts/final-adversarial-webmcp-input-fix.py
      - .github/workflows/final-adversarial-webmcp-input-fix.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: final/adversarial-validation
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: python3 scripts/final-adversarial-webmcp-input-fix.py
      - run: npm ci
      - run: node --experimental-strip-types --test tests/final-adversarial-webmcp-inputs.test.ts tests/phase35d-webmcp-coactivity.test.ts tests/final-adversarial.test.ts
      - run: npm run typecheck
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/webmcp/src/m35d.ts
          git diff --cached --quiet || git commit -m "fix(final): enforce direct WebMCP input schemas"
          git push origin HEAD:final/adversarial-validation
